const nombre = 'Bowie';
const real = 'David Bowie';

const normal = nombre +' ' + real;
const template = `${nombre} ${real}`;

// los template strings sirve para hacer una interpolacion de strings


console.log(normal);
console.log(template);

const html = `
<h1> Hola Mundo </h1>
<p> Lorem ipsum</p>
`
console.log(html);